use std::collections::HashMap;
use std::*;

use dataclasses::dataclass;
struct Rectangle {
    height: int,
    length: int,
}

impl Rectangle {
    fn is_square(&self) -> bool {
        return height == length;
    }
}
